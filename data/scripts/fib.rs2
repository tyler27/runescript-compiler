[proc,fib](int $n)(int)
if ($n = 1) {
    return(0);
}
if ($n <= 2) {
    return(1);
}
return(calc(~fib(calc($n - 1)) + ~fib(calc($n - 2))));

[proc,fib2](int $n)(int)
if ($n = 1) {
    return(0);
}
if ($n = 2) {
    return(1);
}
def_int $a = 0;
def_int $b = 1;
def_int $c;

def_int $count = 2;
while ($count <= $n) {
    $c = calc($a + $b);
    $a = $b;
    $b = $c;
    $count = calc($count + 1);
}
return($c);

[proc,add](int $n, int $n2)(int)
return(calc($n + $n2));